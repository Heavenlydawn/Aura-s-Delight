let typed = new Typed(".auto-type",
{
    strings: ["Savour", "Relish", "Luxuriate"],
    typeSpeed:150,
    backSpeed:150,
    loop:true
});

// sushi
let sushi = new Typed(".auto-sushi",
{
    strings: ["Savour", "Relish", "Luxuriate"],
    typeSpeed:150,
    backSpeed:150,
    loop:true
});

// Ice cream
let ice = new Typed(".auto-ice",
{
    strings: ["Savour", "Relish", "Luxuriate"],
    typeSpeed:150,
    backSpeed:150,
    loop:true
});
// Cake
let cake = new Typed(".auto-cake",
{
    strings: ["Savour", "Relish", "Luxuriate"],
    typeSpeed:150,
    backSpeed:150,
    loop:true
});



// let textIndex = 0;
// let texts = ["Savour!", "Relish!", "Luxuriate"];

// function changeText() {
//   let typingText = document.querySelectorAll(".typing-text");
//   typingText.textContent = texts[textIndex];
//   textIndex = (textIndex + 1) % texts.length;
// }

// setInterval(changeText, 2000);